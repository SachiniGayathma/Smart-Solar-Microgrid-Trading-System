<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartSolarMobileApp">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Prosumer Activities (Person 3) -->
        <activity android:name=".prosumer.LoginActivity" android:exported="false" />
        <activity android:name=".prosumer.RegisterActivity" android:exported="false" />
        <activity android:name=".prosumer.ProsumerDashboardActivity" android:exported="false" />
        <activity android:name=".prosumer.ProfileActivity" android:exported="false" />
        <activity android:name=".prosumer.StationSelectActivity" android:exported="false" />
        <activity android:name=".prosumer.SlotBookingActivity" android:exported="false" />
        <activity android:name=".prosumer.BookingListActivity" android:exported="false" />
        <activity android:name=".prosumer.BookingSummaryActivity" android:exported="false" />
        <activity android:name=".prosumer.QRDisplayActivity" android:exported="false" />
    </application>

</manifest>